---

container_type: "storage"

start_label: "storage"

start_registry: "{{ docker_registry }}"
start_image: "{{ docker_images.storage }}:{{ cluster.build }}"

start_expose:
  - "1234"

start_ports:
  - "{{ hostvars [parent].private_address }}:{{ cluster.ports.storage }}:1234"

start_env:
  HELLO: "world"

start_wait:
  - "{{ cluster.ports.storage }}"

# ex: et ts=2 filetype=yaml
