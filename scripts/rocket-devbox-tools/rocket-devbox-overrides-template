cat <<-END
	---

	deployment_mode: "vagrant"
	deployment_name: "${DEPLOYMENT}"

	developer_name: "${DEVELOPER_NAME}"
	developer_email: "${DEVELOPER_EMAIL}"

	devbox_user: "ubuntu"
	devbox_home: "/home/ubuntu"
	devbox_private: "${DEVBOX_IP}"

	devbox_trusted_hosts:

	  - "${HOST_IP}"

	ubuntu_main_url: "http://archive.ubuntu.com/ubuntu"
	ubuntu_security_url: "http://security.ubuntu.com/ubuntu"

	docker_mirror_url: "https://get.docker.com/ubuntu"
	docker_quay_auth: "${DOCKER_QUAY_AUTH}"

	# ex: et ts=2 filetype=yaml
END

# ex: noet ts=4 filetype=sh
