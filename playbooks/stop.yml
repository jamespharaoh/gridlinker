---

- name: "stop docker containers"
  hosts: "containers"
  gather_facts: "no"
  roles: [ "docker-stop" ]

- name: "destroy docker containers"
  hosts: "containers"
  gather_facts: "no"
  roles: [ "docker-destroy" ]
