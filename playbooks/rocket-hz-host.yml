---

- hosts: "rocket-hz-host"
  roles:

    - "ssh-server-wait"
    - "ssh-server-keyscan"

    - role: "setup-authorized-keys"
      authorized_keys_user: "root"
      authorized_keys_home: "/root"

    - "system-packages-install"
    - "setup-network"
    - "setup-hosts"
    - "setup-sysctl"
    - "install-dnsmasq"

    - "datingnode-host-firewall-setup"
    - "datingnode-host-setup"

    - "python"
    - "etcd"

    - "install-kvm"
    - "install-openvpn"

    - "icinga-core-agent"

# ex: et ts=2 filetype=yaml
