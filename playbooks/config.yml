---

- hosts: "database"
  roles:

    - "config-database"

- hosts: "web-application"
  roles:

    - "config-web-server"
    - "config-web-application"

- hosts: "vhost-proxy"
  roles:

    - "config-vhost-proxy"

# ex: et ts=2 filetype=yaml
