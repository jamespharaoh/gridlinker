---

- name: "create logrotate.conf"
  become: "yes"
  action:

    module: "template"
    dest: "/etc/logrotate.conf"
    src: "hz-host-logrotate-conf"

    owner: "root"
    group: "root"
    mode: "0644"

# ex: et ts=2 filetype=yaml
