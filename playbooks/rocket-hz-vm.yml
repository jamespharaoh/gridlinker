---

- hosts: "rocket-hz-vm"
  roles:

    - "ssh-server-wait"
    - "ssh-server-keyscan"

    - role: "setup-authorized-keys"
      authorized_keys_user: "root"
      authorized_keys_home: "/root"

    - role: "setup-authorized-keys"
      authorized_keys_user: "ubuntu"
      authorized_keys_home: "/home/ubuntu"

    - "system-packages-install"
    - "setup-grub-vm"
    - "setup-network"
    - "setup-hosts"
    - "install-dnsmasq"
    - "install-postfix"
    - "install-nfs-server"

    - "python"

    - "install-docker"
    - "install-lxc"

    - "datingnode-vm-firewall-setup"
    - "setup-datingnode-vm"

    - "icinga-core-agent"

# ex: et ts=2 filetype=yaml
