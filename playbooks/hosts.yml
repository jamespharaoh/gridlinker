---

- hosts: "rocket-hz-host"
  roles: [ "host-hetzner" ]

- hosts: "rocket-hz-vm"
  roles: [ "host-vm" ]

- hosts: "rocket-hz-vhost-proxy"
  gather_facts: "no"
  roles: [ "docker-start" ]

- hosts: "beta"
  roles: [ "host-beta" ]
