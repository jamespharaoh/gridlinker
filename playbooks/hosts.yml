---

- hosts: "rocket-hz-host"
  roles:
    - "host-hetzner"

- hosts: "rocket-hz-vm"
  roles:

    - role: "host-vm"
      tags: "host-vm"

    - role: "setup-hosts"
      tags: "setup-hosts"

- hosts: "rocket-hz-vhost-proxy"
  roles:
    - "docker-start"

- hosts: "beta"
  roles:
    - "host-beta"

# ex: et ts=2 filetype=yaml
