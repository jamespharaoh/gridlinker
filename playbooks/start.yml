---

- name: "common"
  hosts: "hosts"
  gather_facts: "no"
  roles: [ "start-common" ]

- name: "database master"
  hosts: "database-master"
  gather_facts: "no"
  roles: [ "start-database-master" ]

- name: "database slave"
  hosts: "database-slave"
  gather_facts: "no"
  roles: [ "start-database-slave" ]

- name: "web application"
  hosts: "web-application"
  gather_facts: "no"
  roles: [ "start-web-application" ]
