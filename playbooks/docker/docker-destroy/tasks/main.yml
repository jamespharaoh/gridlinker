---

- name: "destroy docker container"
  delegate_to: "host/{{ identity.parent }}"
  action:
    module: "docker"
    name: "{{ identity.name }}"
    image: "whatever"
    state: "absent"

# ex: et ts=2 filetype=yaml
