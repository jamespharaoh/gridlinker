---

- hosts: "base-build"
  roles:

    - "update-system-packages"
    - "install-system-packages"
    - "build-common"

- hosts: "database-build"
  roles:

    - "update-system-packages"
    - "install-system-packages"
    - "build-common"
    - "build-database"

- hosts: "web-server-build"
  roles:

    - "update-system-packages"
    - "install-system-packages"
    - "build-common"
    - "build-web-server"

- hosts: "web-application-build"
  roles:

    - "update-system-packages"
    - "install-system-packages"
    - "build-common"
    - "build-web-server"
    - "build-web-application"

- hosts: "vhost-proxy-build"
  roles:

    - "update-system-packages"
    - "install-system-packages"
    - "build-common"
    - "build-vhost-proxy"

- hosts: "storage-build"
  roles:

    - "update-system-packages"
    - "install-system-packages"
    - "build-common"
    - "build-storage"

- hosts: "memcache-build"
  roles:

    - "update-system-packages"
    - "install-system-packages"
    - "build-common"
    - "build-memcache"

# ex: et ts=2 filetype=yaml
