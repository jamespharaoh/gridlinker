---

- name: "configure network"
  tags: "network"
  sudo: "yes"
  action:

    module: "template"
    dest: "/etc/network/interfaces"
    src: "setup-network-interfaces"

# ex: et ts=2 filetype=yaml
