---

identity:

  name: "super-cluster"
  type: "class"

class:

  namespace: "cluster"

  groups:

    - "super-clusters"

icinga: {}

icinga_core:

  database_type: "postgresql"
  database_delegate: "host/{{ icinga_core_database_master }}"
  database_username: "icinga"
  database_hostname: "{{ icinga_core_database_master }}.vpn"
  database_port: "5432"
  install_version: ""

icinga_web_core:

  config_groups: "{{ icinga_web_core_groups | values }}"
  config_users: "{{ icinga_web_core_users | values }}"

  database_type: "postgresql"
  database_delegate: "host/{{ icinga_core_database_master }}"
  database_username: "icingaweb"
  database_hostname: "{{ icinga_core_database_master }}.vpn"
  database_port: "5432"

  install_version: ""

postfix: {}

ssh_user: {}

super_cluster:

  icinga_core_database_hostname: "{{ icinga_core_database_hostname }}"
  icinga_core_database_master: "{{ icinga_core_database_master }}"
  icinga_core_database_password: "{{ icinga_core_database_password }}"
  icinga_core_database_username: "{{ icinga_core_database_username }}"
  icinga_core_install_version: "{{ icinga_core_install_version }}"
  icinga_core_ticket_salt: "{{ icinga_core_ticket_salt }}"

  icinga_web_core_admin_password: "{{ icinga_web_core_admin_password }}"
  icinga_web_core_database_hostname: "{{ icinga_web_core_database_hostname }}"
  icinga_web_core_database_password: "{{ icinga_web_core_database_password }}"
  icinga_web_core_database_port: "{{ icinga_web_core_database_port }}"
  icinga_web_core_database_type: "{{ icinga_web_core_database_type }}"
  icinga_web_core_database_username: "{{ icinga_web_core_database_username }}"
  icinga_web_core_install_version: "{{ icinga_web_core_install_version }}"

# ex: et ts=2 filetype=yaml
