---

identity:

  name: "rocket-hz-sites"
  type: "class"

class:

  scope: "global"

  groups:

    - "rocket-hz"

ansible:

  ssh_host: "{{ private.address }}"
  ssh_user: "ubuntu"

ssh:

  hostnames:
    - "{{ identity.name }}"
    - "{{ private.address }}"

# ex: et ts=2 filetype=yaml
