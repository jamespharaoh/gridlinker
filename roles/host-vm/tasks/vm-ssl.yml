---

- name: "upload ssls"
  connection: "local"
  shell:
    rsync \
      --archive \
      /datingnode-ssl/ \
      root@{{ public_address }}:/datingnode-ssl/
  sudo: "yes"

# ex: et ts=2 filetype=yaml
