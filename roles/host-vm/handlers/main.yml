---

- name: "destroy vhost proxy"
  action:
    module: "docker"
    name: "vhost-proxy"
    state: "absent"
    image: "whatever"
  sudo: "yes"

- name: "update grub"
  shell: "update-grub"
  sudo: "yes"

# ex: et ts=2 filetype=yaml
