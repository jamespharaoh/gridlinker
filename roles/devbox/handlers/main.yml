---

- name: "reload nfs server"
  action:
    module: "service"
    name: "nfs-kernel-server"
    state: "reloaded"
  sudo: "yes"
  sudo_user: "root"

- name: "restart dnsmasq"
  action:
    module: "service"
    name: "dnsmasq"
    state: "restarted"
  sudo: "yes"

# ex: et ts=2 filetype=yaml
