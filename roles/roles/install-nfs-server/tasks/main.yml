---

- name: "install nfs packages"
  action:
    module: "apt"
    name: "{{ item }}"
  sudo: "yes"
  with_items:
    - "nfs-common"
    - "nfs-kernel-server"

- meta: "flush_handlers"

# ex: et ts=2 filetype=yaml
