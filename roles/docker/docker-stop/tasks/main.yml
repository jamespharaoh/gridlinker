---

- name: "stop docker container"
  delegate_to: "host/{{ identity.parent }}"
  action:
    module: "docker"
    name: "{{ identity.name }}"
    image: "whatever"
    state: "stopped"

# ex: et ts=2 filetype=yaml
