{{ ansible_warning ['#'] }}

{% for task in btrfs_dedupe_tasks %}
{{ [
  task.schedule,
  task.user,
  '/usr/local/bin/btrfs-dedupe-script',
  '/etc/btrfs-dedupe-config',
  task.name,
] | join (' ') }}
{% endfor %}

# ex: et ts=2 filetype=cron
