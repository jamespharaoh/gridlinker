---

- name: "install vhost proxy packages"
  with_items:

    - "nginx"

  sudo: "yes"
  action:

    module: "apt"
    name: "{{ item }}"

- name: "create nginx service directory"
  sudo: "yes"
  action:

    module: "file"
    path: "/etc/service/nginx"
    state: "directory"

- name: "create nginx service script"
  sudo: "yes"
  action:

    module: "template"
    dest: "/etc/service/nginx/run"
    src: "nginx-service"

    mode: "0755"

# ex: et ts=2 filetype=yaml# ex: et ts=2 filetype=yaml
